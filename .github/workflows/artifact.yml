name: Package artifact
on:
  workflow_dispatch:
jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: nba-analysis-package
          path: NBA_Sports_Analytics_Analysis_Package.zip
          retention-days: 1
